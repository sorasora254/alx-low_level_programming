#include <stdio.h>
/**
 * main - This is an entry point
 * Descrition: Using printf
 * Return: always returning zero
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
